public class UserPlayer extends User{
    public UserPlayer(String nombre,String nick, String password) {
        this.setName(nombre);
        this.setPassword(password);
        this.setNick(nick);
    }

}
